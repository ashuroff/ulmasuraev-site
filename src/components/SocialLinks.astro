---
import Icon from "./Icon.astro";

interface Props {
  variant?: "circle" | "text";
  class?: string;
}

const { variant = "circle", class: className = "" } = Astro.props;

const links = [
  { name: "instagram" as const, href: "https://www.instagram.com/ulmas_uraev/", label: "Instagram" },
  { name: "youtube" as const, href: "https://www.youtube.com/@ulmasurayev", label: "YouTube" },
  { name: "telegram" as const, href: "https://t.me/ulmas_uraev", label: "Telegram" },
  { name: "imdb" as const, href: "https://www.imdb.com/name/nm14050582/?ref_=ext_shr_lnk", label: "IMDb" },
];
---

{
  variant === "circle" ? (
    <div class={`flex items-center gap-3 ${className}`}>
      {links.map((l) => (
        <a href={l.href} target="_blank" rel="noopener noreferrer" aria-label={l.label} class="icon-circle">
          <Icon name={l.name} class="w-4 h-4" />
        </a>
      ))}
    </div>
  ) : (
    <div class={`flex items-center gap-4 font-accent text-xs uppercase tracking-[0.1em] ${className}`}>
      {links.map((l, i) => (
        <>
          <a href={l.href} target="_blank" rel="noopener noreferrer" class="text-gold hover:text-[var(--color-gold-hover)] transition-colors">
            {l.label}
          </a>
          {i < links.length - 1 && <span class="text-muted">·</span>}
        </>
      ))}
    </div>
  )
}
